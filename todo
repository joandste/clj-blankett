make frontend prettier
add timed forms
(consider client side rendering with clojurescript)
show name of form
authentication
ability to customize body when making form
move templates to own directory, set template path relative to executable
(extend clojure to be able to grab values from keys in maps nested in a vector using macros)
cleanup all the datastructures, especially the nesting in a vector when there is a single result, multi results can be cleaned using macros
also learn to use macros :D (its a bit hard)