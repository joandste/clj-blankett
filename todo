make frontend prettier
add timed forms
(consider client side rendering with clojurescript)
show name of form
authentication
ability to customize body when making form
move templates to own directory, set template path relative to executable
learn 2 macros :D
make unwrapping a sql response with one element more elegant (find way to never need to unwrap?)
make utility to create database, so it doesn't have to be made from a REPL